<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alterindonesia.project.com">

    <application
        android:name=".Utils.StoreObjek"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashScreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgotActivity" />
        <activity android:name=".TopUpActivity" />
        <activity android:name=".TransferActivity" />
        <activity android:name=".TerimaUangActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".UbahPasswordActivity" />
        <activity android:name=".ListMerchantActivity" />
        <activity android:name=".MerchantDetailActivity" />
        <activity android:name=".RiwayatActivity"></activity>
    </application>

</manifest>